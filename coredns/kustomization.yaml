apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kube-system
patches:
  - target:
      kind: Deployment
      name: coredns
    patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: coredns
      spec:
        replicas: 1
